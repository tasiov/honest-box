<% if @current_user %>

	Hey, <%= @current_user.name %>, you're already logged in! <%= link_to "Logout!", logout_path %>

<% else %>

	<%= form_tag "/login" do %> 
		name: <%= text_field_tag :name %>
		email: <%= text_field_tag :email %>
		<%= submit_tag "Log In" %>
	<% end %>

<% end %>